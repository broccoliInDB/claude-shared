#!/bin/bash
# SessionStart hook (matcher: "compact"): 컨텍스트 압축 후 핵심 규칙 복원
# 긴 세션에서 컨텍스트가 압축될 때 유실되면 안 되는 핵심 규칙을 재주입
#
# stdout → Claude의 컨텍스트에 추가됨
# exit 0: 항상

cat << 'CONTEXT'
=== 핵심 컨텍스트 복원 (컨텍스트 압축 후 자동 주입) ===

[커밋] 반드시 /p-commit 사용. 직접 git commit 금지 (Hook이 차단함).
[판단] "할까요?" 금지. 판단할 수 있으면 판단하고 진행. 어려울 때만 선택지 + 근거 제시.
[학습] .claude/rules/ 파일이 정본. 로컬 메모리는 스테이징 영역.
[역할] 사용자 = 오너(방향). AI = 시니어 집단(보완, 개선, 고도화). AI는 스스로 발전해야 함.
[리뷰] 리뷰 + 회고 = AI의 발전 메커니즘. 건너뛸 수 없음.
[발전] 반복 패턴 2회 이상 → /evolve로 팀 규칙 승격 제안.

===
CONTEXT

exit 0
